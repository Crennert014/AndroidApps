<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidApps">

        <!-- Splash / Welcome Screen (launcher entry point) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidApps.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Menu -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- Feature Activities -->
        <activity android:name=".Feature1Activity" android:exported="false" />
        <activity android:name=".Feature2Activity" android:exported="false" />
        <activity android:name=".Feature3Activity" android:exported="false" />
        <activity android:name=".Feature4Activity" android:exported="false" />
        <activity android:name=".Feature5Activity" android:exported="false" />

    </application>

</manifest>
