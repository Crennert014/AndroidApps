<?xml version="1.0" encoding="utf-8"?>
<!-- activity_feature1.xml — Placeholder layout for Feature 1 -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/feature_background">

    <!-- App bar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/feature_1_title" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Main content area -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- TODO: Replace / add your content below this comment -->

            <TextView
                android:id="@+id/featureDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/feature_1_description"
                android:textSize="16sp"
                android:lineSpacingExtra="4dp" />

            <!-- Spacer -->
            <Space
                android:layout_width="match_parent"
                android:layout_height="32dp" />

            <!-- Back to menu button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnBackToMenu"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/back_to_menu" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
